# @wendoo/bridge-protocol

WebSocket protocol types and schemas for the Wendoo bridge.

Defines the message types, validation schemas, and notification types shared between
the bridge session engine (`bridge-session`) and the relay services built on it, the
client SDK (`bridge-client`), and app-level packages.

## Install

```sh
npm install @wendoo/bridge-protocol
```

## License

MIT
