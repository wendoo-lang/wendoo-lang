/** Session kind of every connection the service hands the session engine. */
export const SESSION_KIND = "vscode";

/** Role of a Wendoo app's connection, made at the `/app` route. */
export const APP_ROLE = "app";

/** Role of a VS Code extension's connection, made at the `/extension` route. */
export const EXTENSION_ROLE = "extension";
